---
"@subbly/plugin-lint": patch
---

Agent tool allowlist follows the builder: `web_search` and `web_fetch` replace `scrape_webpage`, `apply_patch` is grantable, and `tool_run`, `tool_search`, `tool_describe` replace `mcp_run_tool`.
