Run the plugin's hello script with `execute_command`: `node scripts/hello.js`.

Append one line to `greetings.log` in your state directory: the current ISO timestamp, a space, and the script output. Create the file if it does not exist. Never edit or delete earlier lines.

Do not change any project files. Finish with a one-sentence summary of what the script printed.
